# Why gyms sell more memberships than they can fit

*   **System:** Low utilization / the no-show subsidises the regular
*   **Narrator:** Bright, energetic adult-female (en-US-AvaNeural)
*   **Palette:** `warm amber` for YOU / desire; `coral red` for naive assumption / perceived problem; `signal blue` for the revealed system / capacity.
*   **Hero Element:** An `amber` stick-figure "YOU" and a `gym space` represented by abstract shapes (machines, floor area) that transforms to show capacity and usage.

| # | Time | Story beat | Stick-figure scene | Motion / camera / transition | English VO | SFX |
|---|---|---|---|---|---|---|
| 1 | 0–10s | **YOU + NEED**: Desiring a good deal and access. | `amber` stick-figure "YOU" stands, thinking of a `warm amber` dollar sign and a `warm amber` dumbbell. | `YOU` appears center. Thought bubbles animate in with `click-soft.mp3`. Camera gently pushes in on `YOU`. A `whoosh.mp3` as the concept of a gym appears around `YOU`. | You signed up for a gym membership, hoping to get fit and use the equipment whenever you want for a reasonable price. | `click-soft.mp3`, `whoosh.mp3` |
| 2 | 10–20s | **GO**: Encountering the reality of peak crowds. | `YOU` (amber) is now in a crowded gym, represented by many other `amber` stick-figures densely packed around basic equipment shapes. Some equipment is `coral red` indicating unavailability. | Camera pans slightly to reveal more crowded `amber` figures. Equipment lights up `coral red` as if "taken". `whoosh.mp3` for the crowd appearance. The scene builds tension. `stack-collapse.mp3` as the idea of "easy access" collapses. | But sometimes, you arrive to find it completely packed, with every machine occupied and long waits for free weights. | `whoosh.mp3`, `stack-collapse.mp3` |
| 3 | 20–30s | **SEARCH**: Confusion about the business model. | `YOU` (amber) stands amidst the crowded scene, scratching head, with `coral red` question marks floating around. A simple `coral red` bar fills up to represent "sold memberships" exceeding a `white` "physical capacity" bar. | `YOU` looks around, confused. The `coral red` question marks appear with `focus.mp3`. The capacity bar visualizes the naive thought process, filling past the limit with `click-soft.mp3`. | You might wonder how they can possibly sustain a business if it's always this full, or why they sold so many spots. | `focus.mp3`, `click-soft.mp3` |
| 4 | 30–40s | **FIND**: The reveal of low utilization. | The scene transforms into a clear gym layout. A `signal blue` grid shows maximum capacity. Only a few `amber` stick-figures are actively using it. Many `faded white` ghost figures represent paying members who are absent. | The `coral red` bar collapses (implied). The `signal blue` grid appears and expands with `focus.mp3`. `amber` figures move dynamically. `faded white` ghost figures fade in, showing the true ratio with `transform.mp3`. | The invisible system is simple: most members pay but rarely show up, meaning the gym's actual capacity is rarely tested. | `focus.mp3`, `transform.mp3` |
| 5 | 40–50s | **TAKE**: The cost – the illusion of full access. | `YOU` (amber) looks at a scale balancing a small group of `amber` figures (regulars) on one side and a large stack of `faded white` figures (no-shows) on the other. The `amber` side is weighted down by a `signal blue` 'affordable price' tag. | The scale visual appears, tilting with `whoosh.mp3`. The `signal blue` tag appears with `click-soft.mp3`. Camera focuses on the balance. | Your affordable membership is only possible because the thousands who pay and don't come subsidize the few who train regularly. | `whoosh.mp3`, `click-soft.mp3` |
| 6 | 50–60s | **RETURN + CHANGE**: A new understanding. | `YOU` (amber) is back in a gym, but now looking knowing and calm. The gym is moderately busy, with `amber` figures and `faded white` ghosts understood to be part of the system. A subtle `signal blue` aura surrounds the gym. | `YOU` nods slightly. The gym environment reappears, but now the `signal blue` underlying system is subtly visible with `complete-done.mp3`. Camera slowly pulls back. `whoosh-fast.mp3` as a final thought. | Now, when you see a crowded gym, you understand it's not oversold, but perfectly balanced by the unseen majority. | `complete-done.mp3`, `whoosh-fast.mp3` |

**Continuity map:** The `amber` stick-figure "YOU" is present in all scenes. The abstract `gym space` (represented by equipment shapes and capacity grids) is the persistent environment, transforming to reflect crowding, capacity, and the underlying system.
